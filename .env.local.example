# Database Backend Selection
# ===========================
# Leave Supabase variables empty to use local SQLite (default for local development)
# Set all three variables to use Supabase (required for Vercel deployment)

# Supabase Configuration (Production/Vercel)
# Get these values from your Supabase project dashboard (https://app.supabase.com)

# Project Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Project Settings > API > Project API keys > anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Project Settings > API > Project API keys > service_role key (keep this secret!)
# Required for server-side operations that bypass Row Level Security
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================
# SQLite is used automatically when Supabase is not configured
# This is the default for local development - no configuration needed!
